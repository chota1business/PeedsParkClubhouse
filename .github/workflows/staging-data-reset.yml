name: Staging data reset (not configured)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo '::error::No data reset is configured. No data has been deleted. Use a reviewed staging-only reset script.'
          exit 1
