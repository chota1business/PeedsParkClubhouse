name: Staging deployment location
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  staging-link:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Staging runs at 9 PM IST in its own Pages repository.' >> "$GITHUB_STEP_SUMMARY"
          echo '[Open staging deployment](https://github.com/chota1business/PeedsParkClubhouse-Staging/actions/workflows/staging-deploy.yml)' >> "$GITHUB_STEP_SUMMARY"
